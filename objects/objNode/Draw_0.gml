/*var _col = c_white;//draw_set_color(c_white);

if (array_get_index(objActor.path, id) != -1){
	_col = c_lime;
}

draw_sprite_ext(sprite_index, image_index, x, y, 1, 1, 0, _col, 1);
//draw_self();

//draw_set_color(c_white);
//draw_text(x, y, string(array_length(neighbours)));